<script>
	export let data;
	console.log(data);
</script>

<p>try here</p>

<!---
<section>
	{#each data.images as image}
		<img src={image.logo.url} alt="" />
	{/each}
</section>



{#each media as { name, alt }}
	<div class="flex flex-col justify-around mx-2 md:mx-5">
		<p class="font-bold">{name}</p>
		<p class="font-bold">{alt}</p>
	</div>
{/each}
-->
